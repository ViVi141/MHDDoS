# Memcached反射器列表
# 格式：每行一个IP地址
# 这些是开放Memcached服务的服务器IP，用于MEM放大攻击
# 注意：使用反射器攻击可能涉及法律问题，仅用于授权的安全测试

# 说明：
# 1. 每行一个IP地址，格式：IP（不需要端口）
# 2. Memcached放大倍数极高（10000-50000倍），请谨慎使用
# 3. 可以使用扫描工具查找开放Memcached服务的服务器（默认端口11211）
# 4. 建议使用大量反射器IP（100+）即可获得极好的效果
# 5. 注意：Memcached放大攻击威力极大，可能导致严重后果


# NTP反射器列表
# 格式：每行一个IP地址
# 这些是开放NTP服务的服务器IP，用于NTP放大攻击
# 注意：使用反射器攻击可能涉及法律问题，仅用于授权的安全测试

# 示例NTP服务器（请替换为实际的开放NTP服务器IP）
# pool.ntp.org
# time.google.com
# time.cloudflare.com

# 说明：
# 1. 删除上面的注释行，只保留IP地址或域名
# 2. 每行一个IP地址或域名，格式：IP或域名（不需要端口）
# 3. 可以使用扫描工具查找开放NTP服务的服务器
# 4. NTP放大倍数极高（200-1000倍），请谨慎使用
# 5. 建议使用大量反射器IP（1000+）以获得更好的效果

